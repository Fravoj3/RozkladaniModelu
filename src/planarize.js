const normalizedObject(verts, faces, faceNormals)=>{
    if (faces.length != faceNormals.length) {
        for (let i = 0; i < faces.length; i++) {
            for (let j = 1; j < faces[i].length; j++) {

            }
        }
    }
}